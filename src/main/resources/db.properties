# ================================================
# NhietDoiXanh - SQL Server Database Configuration
# ================================================
db.url=jdbc:sqlserver://14.225.217.109;databaseName=BanNuoc_Truc;encrypt=true;trustServerCertificate=true
db.username=sa
db.password=TOP1@iyounguru!
db.driver=com.microsoft.sqlserver.jdbc.SQLServerDriver

# ================================================
# Email SMTP (dùng gửi OTP quên mật khẩu) — điền thông tin thật trước khi deploy
# ================================================
mail.smtp.host=smtp.gmail.com
mail.smtp.port=587
mail.smtp.username=
mail.smtp.password=

# ================================================
# PayOS (thanh toán VietQR) — lấy từ dashboard.payos.vn
# Ưu tiên set qua biến môi trường (PAYOS_CLIENT_ID, PAYOS_API_KEY, PAYOS_CHECKSUM_KEY,
# PAYOS_RETURN_URL, PAYOS_CANCEL_URL, PAYOS_WEBHOOK_URL) khi deploy — các dòng dưới đây
# chỉ là fallback cho local dev. Để trống thì PayOS tự động bị ẩn ở checkout, COD vẫn hoạt động.
# ================================================
payos.client_id=
payos.api_key=
payos.checksum_key=
payos.return_url=
payos.cancel_url=
payos.webhook_url=
