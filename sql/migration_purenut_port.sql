/* ================================================================
   FILE NÀY ĐÃ NGƯNG DÙNG — KHÔNG CHẠY.
   Được viết dựa trên giả định SAI về schema DB (tưởng Orders là bảng
   đơn giản cũ, sẽ đổi tên nhầm bảng Orders thật đang chứa dữ liệu).

   Dùng thay: sql/migration_purenut_port_v2.sql
   ================================================================ */
